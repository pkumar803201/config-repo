server:
  port: 8084

spring:
  application:
    name: ai-service

  config:
    import: optional:configserver:http://localhost:8888

  ai:
    google:
      genai:
        api-key: YOUR_GEMINI_API_KEY   # ✅ FIXED location

    chat:
      options:
        model: gemini-1.5-flash        # ✅ FIXED location

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka   # ✅ FIXED
