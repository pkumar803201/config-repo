server:
  port: 8084

spring:
  application:
    name: ai-service

  config:
    import: optional:configserver:http://localhost:8888

  ai:
    google:
      api-key: AlzaSyCMCq357na4bHRSRzprcyGSqGk1xflkskQ   # ✅ Put your key here

      gemini:
        model: gemini-1.5-flash   # ✅ fast + cheap + good

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eurek
